import React from 'react';
import { NavBar } from './NavBar';

export const Header: React.FC = () => {
    return (
        <div>
            <NavBar title={''} links={[]} />
        </div>
    );
};
